import {Express} from "express";
import {UserService} from "../service/user";
import {CareerService} from "../service/career";

export const setupCareerRoutes = (app: Express, userService: UserService, careerService: CareerService) => {
}
